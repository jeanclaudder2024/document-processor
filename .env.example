# Document Processor (Python) - Environment variables
# Copy this file to .env and fill in your values:  cp .env.example .env

# ============ Supabase (required for DB and document generation) ============
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-anon-or-service-role-key

# Optional: use service role key for full admin access (bypass RLS)
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ============ Optional: OpenAI (for AI-generated placeholder values) ============
# OPENAI_API_KEY=sk-your-openai-key

# ============ Port (optional) ============
# VPS: default 8000 (Nginx proxies /api to localhost:8000). Local dev: set PORT=5000
# PORT=8000
